// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
    provider = "prisma-client-js"
}

datasource db {
    provider = "postgresql"
    url      = env("DATABASE_URL")
}

model Address {
    id        String   @id @default(cuid())
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    inCareOfName  String?
    streetAddress String?
    unitType      String?
    unitNumber    String?
    cityTown      String?
    state         String?
    zipCode       String?
    province      String?
    postalCode    String?
    country       String  @default("US")

    broker Broker? @relation(name: "brokerAddresses", fields: [brokerId], references: [id])
    brokerId String?
}

enum ProcessingStatus {
    CREATED
    PROCESSING
    COMPLETED
    ERROR
}

model Broker {
    id String @id @default(uuid())
    name String
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    email String @unique
    phone String?

    processingStatus ProcessingStatus @default(CREATED)

    // Brokerage address
    inCareOfName  String?
    streetAddress String?
    unitType      String?
    unitNumber    String?
    cityTown      String?
    state         String?
    zipCode       String?
    province      String?
    postalCode    String?
    country       String  @default("US")

    properties Address[] @relation(name: "brokerAddresses")

    documents Document[] @relation(name: "brokerDocuments")

    metadata    Json?

    @@index([name])
}

model Document {
    id String @id @default(cuid())
    createdAt DateTime @default(now())
    updatedAt DateTime @updatedAt

    processingStatus ProcessingStatus @default(CREATED)

    name String
    type String?
    fullText String?
    fileHash String?

    objectKey String
    thumbnailKey String?

    brokerId String?
    broker Broker? @relation(name: "brokerDocuments", fields: [brokerId], references: [id])
}